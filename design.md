# 校园二手交易微信小程序 — 设计文档

## 一、项目概述

**定位**：面向高校师生的校园二手闲置物品交易平台（微信小程序端），简历作品/实习演示级质量。

**核心价值**：学生在校园内快速发布闲置、浏览商品、在线沟通、完成交易，打造轻量级校内 C2C 二手市场。

**设计风格**：参考闲鱼 App 暖黄色调，营造温暖、活泼、亲和的二手交易氛围，降低用户距离感。

**当前阶段**：纯前端小程序（Mock 数据），跑通全部页面交互，后续对接 SpringBoot 后端。

---

## 二、技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | 微信小程序原生 | WXML + WXSS + JS，不引入第三方框架 |
| 状态管理 | 全局 App + Page data | 轻量级，无需 Vuex/Redux |
| 网络请求 | `wx.request` 封装 + Mock 拦截 | 当前用 Mock，后续替换真实 API |
| UI 风格 | 闲鱼风格暖黄色调 | 卡片式布局，温暖活泼的二手交易氛围 |
| 持久化 | `wx.Storage` | 本地存储聊天记录、收藏、草稿 |
| 图片 | `wx.chooseImage` + 本地临时路径 | Mock 阶段存本地，后续上传云存储 |

---

## 三、页面架构 & 导航设计

### 3.1 Tab Bar（底部导航栏）

```
┌──────────┬──────────┬──────────┬──────────┐
│   🏠     │   ➕     │   💬     │   👤     │
│   首页   │   发布   │   消息   │   我的   │
└──────────┴──────────┴──────────┴──────────┘
```

### 3.2 完整页面清单（12 页）

| 页面 | 路径 | 类型 | 功能描述 |
|------|------|------|----------|
| 首页 | `pages/index/index` | Tab | 分类导航、商品瀑布流、搜索入口、下拉刷新 |
| 发布页 | `pages/publish/publish` | Tab | 商品信息表单、图片上传、分类选择 |
| 消息列表 | `pages/chat-list/chat-list` | Tab | 私信会话列表、未读红点、最后消息预览 |
| 个人中心 | `pages/profile/profile` | Tab | 用户头像昵称、我发布的、我买的、我收藏的、设置 |
| 商品详情 | `pages/product/detail` | 普通 | 图片轮播、价格、描述、卖家卡片、联系/收藏/购买 |
| 搜索页 | `pages/search/search` | 普通 | 关键词搜索、历史搜索、热门搜索、搜索结果列表 |
| 聊天详情 | `pages/chat/detail` | 普通 | 一对一聊天窗口、消息气泡、发送文字/图片 |
| 订单列表 | `pages/order/list` | 普通 | 筛选 Tab（全部/待付款/待发货/已完成）、订单卡片 |
| 订单详情 | `pages/order/detail` | 普通 | 订单状态流、商品信息、价格明细、操作按钮 |
| 我的发布 | `pages/my-publish/list` | 普通 | 我发布的商品、上架/下架管理、编辑/删除 |
| 我的收藏 | `pages/my-favorite/list` | 普通 | 收藏的商品列表、取消收藏 |
| 关于页 | `pages/about/about` | 普通 | 项目介绍、版本号、技术栈展示 |

### 3.3 页面跳转关系

```
首页 ──→ 搜索页
  │──→ 商品详情 ──→ 聊天详情
  │         │──→ 订单详情
  │
发布页 ──→ 商品详情（发布成功后）
  │
消息列表 ──→ 聊天详情 ──→ 商品详情
  │
个人中心 ──→ 我的发布 ──→ 编辑（跳发布页）
  │──→ 我买到的（订单列表）
  │──→ 我卖出的（订单列表）
  │──→ 我的收藏 ──→ 商品详情
  │──→ 关于页
```

---

## 四、功能模块详细设计

### 4.1 首页模块
- **顶部搜索栏**：固定顶部，点击跳转搜索页
- **分类横滑栏**：`全部 | 数码 | 书籍 | 生活 | 服饰 | 美妆 | 运动 | 其他`
- **商品瀑布流**：双列卡片，懒加载，下拉刷新，触底加载更多
- **商品卡片信息**：封面图、标题、价格（¥红色突出）、卖家头像+昵称、发布时间
- **排序切换**：最新发布 / 价格升序 / 价格降序

### 4.2 商品发布模块
- **图片上传**：最多 9 张，支持预览、删除、拖拽排序（简化版：长按删除）
- **表单字段**：
  - 商品标题（必填，2-30字）
  - 商品描述（必填，10-500字，富文本简化版）
  - 价格（必填，数字，保留两位小数，¥符号前缀）
  - 原价（选填，用于展示折扣力度）
  - 分类（必填，从分类列表选择）
  - 新旧程度（必填：全新/几乎全新/轻微使用/正常使用）
  - 交易方式（必填：自提/快递/都可以）
- **发布按钮**：底部固定，表单校验通过后亮起

### 4.3 商品详情模块
- **图片轮播**：swiper 组件，支持预览大图
- **价格区**：现价（大号红色）、原价（删除线灰色）、发布时间
- **商品信息**：标题、描述、分类标签、新旧程度、交易方式
- **卖家卡片**：头像、昵称、信誉分（Mock 展示）、在售商品数、TA 的在售商品横滑
- **底部操作栏**：
  - 收藏按钮（星形图标，已收藏/未收藏状态）
  - 联系卖家按钮（跳聊天页）
  - 立即购买按钮（跳订单确认页 → 订单详情）

### 4.4 即时通讯模块（简化版）
- **聊天列表**：会话项显示头像、昵称、最后一条消息、时间、未读红点
- **聊天窗口**：
  - 消息气泡（对方左对齐灰底、自己右对齐暖黄底）
  - 发送文字消息
  - 发送图片（拍照/相册）
  - 自动滚动到底部
  - 消息本地持久化存储
- **Mock 模式**：预置 2-3 个模拟对话，支持自己发消息

### 4.5 订单模块
- **订单状态流**：待付款 → 待发货 → 待收货 → 已完成 / 已取消
- **订单卡片**：商品缩略图、标题、价格、状态标签（不同颜色）、操作按钮
- **订单详情**：完整商品信息、买卖双方信息、价格明细（商品金额+运费）、订单编号、时间线
- **操作按钮**：
  - 待付款：取消订单 | 去付款（Mock）
  - 待发货：无（等待卖家发货）
  - 待收货：确认收货
  - 已完成：删除订单 | 再次购买

### 4.6 个人中心模块
- **头部**：背景渐变、头像、昵称、个人简介
- **数据统计**：在售 / 已售 / 收藏 数量
- **功能列表**（分组卡片式）：
  - 我的交易：我发布的、我买到的、我卖出的
  - 我的收藏、浏览历史
  - 设置：关于我们、清除缓存、意见反馈

---

## 五、数据模型设计（Mock 阶段）

### 5.1 商品（Product）
```js
{
  id: String,           // 商品ID
  title: String,        // 标题
  description: String,  // 描述
  price: Number,        // 售价
  originalPrice: Number,// 原价
  images: [String],     // 图片路径数组
  category: String,     // 分类
  condition: String,    // 新旧程度
  tradeType: String,    // 交易方式
  seller: {             // 卖家信息
    id, avatar, nickname, creditScore, productCount
  },
  status: String,       // 在售/已售/已下架
  viewCount: Number,    // 浏览量
  favoriteCount: Number,// 收藏数
  createTime: String    // 发布时间
}
```

### 5.2 订单（Order）
```js
{
  id: String,
  product: { ... },     // 关联商品快照
  buyer: { ... },       // 买家信息
  seller: { ... },      // 卖家信息
  status: String,       // pending/paid/shipped/received/completed/cancelled
  price: Number,        // 成交价
  freight: Number,      // 运费（默认0）
  totalPrice: Number,   // 实付金额
  createTime: String,
  statusTimeline: [{ status, time }]  // 状态变更时间线
}
```

### 5.3 聊天消息（Message）
```js
{
  id: String,
  conversationId: String, // 会话ID（两个用户ID拼接）
  fromUserId: String,
  toUserId: String,
  type: String,          // text/image
  content: String,       // 文字内容或图片路径
  createTime: String,
  isRead: Boolean
}
```

---

## 六、UI 设计规范

### 6.1 色彩系统（闲鱼风暖黄色调）

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | `#FFD100` | 闲鱼黄，按钮、标签、导航高亮 |
| 主色深 | `#F5C400` | 按下态、渐变深色 |
| 主色浅 | `#FFF3CD` | 标签背景、高亮区域底色 |
| 价格红 | `#FF4D4F` | 价格展示，醒目但不刺眼 |
| 文字主 | `#1A1A1A` | 标题、正文 |
| 文字辅 | `#666666` | 描述、时间、次要信息 |
| 文字弱 | `#999999` | 提示文字、占位符 |
| 背景 | `#F5F5F5` | 页面底色 |
| 卡片白 | `#FFFFFF` | 卡片、列表项 |
| 分割线 | `#EEEEEE` | 细线分隔 |
| 渐变辅 | `#FFE57F` | 头部/按钮渐变辅助色 |
| 警告橙 | `#FF9800` | 待处理状态标签 |
| 成功色 | `#52C41A` | 已完成/在售状态 |

### 6.2 尺寸规范
- 页面左右边距：`30rpx`
- 卡片圆角：`16rpx`
- 按钮圆角：`8rpx`
- 头像尺寸：小 `64rpx` / 中 `80rpx` / 大 `120rpx`
- 图标尺寸：`36rpx` ~ `48rpx`
- 商品卡片宽度：`(屏幕宽 - 90rpx) / 2`

### 6.3 字体规范
- 大标题：`36rpx` bold
- 标题：`32rpx` medium
- 正文：`28rpx` regular
- 辅助文字：`24rpx` regular
- 价格大字：`36rpx` bold（红色）

---

## 七、目录结构（最终交付）

```
campus-secondhand/
├── app.js                        # 小程序入口
├── app.json                      # 全局配置（页面路由、TabBar、窗口样式）
├── app.wxss                      # 全局样式（颜色变量、通用类）
├── project.config.json           # 项目配置
├── sitemap.json                  # 站点地图
│
├── pages/
│   ├── index/                    # 首页
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── publish/                  # 发布商品
│   ├── product/                  # 商品详情
│   ├── search/                   # 搜索
│   ├── chat-list/                # 消息列表
│   ├── chat/                     # 聊天详情（子目录 detail/）
│   ├── order/                    # 订单（list/ + detail/）
│   ├── profile/                  # 个人中心
│   ├── my-publish/               # 我的发布
│   ├── my-favorite/              # 我的收藏
│   └── about/                    # 关于
│
├── components/                   # 公共组件
│   ├── product-card/             # 商品卡片
│   ├── empty-state/              # 空状态占位
│   ├── loading/                  # 加载动画
│   └── auth-modal/               # 授权弹窗
│
├── utils/
│   ├── util.js                   # 工具函数（时间格式化等）
│   ├── mock.js                   # Mock 数据生成器
│   ├── storage.js                # 本地存储封装
│   └── constants.js              # 常量定义（分类、状态等）
│
└── images/                       # 图标资源（tabbar 图标等）
```

---

## 八、开发策略

### 阶段划分

| 阶段 | 内容 | 预计产出 |
|------|------|----------|
| **P1** | 全局配置 + TabBar + 首页（Mock数据） | 可浏览商品列表 |
| **P2** | 商品详情 + 搜索 | 可查看详情、搜索 |
| **P3** | 发布商品 + 个人中心 | 可发布、管理个人信息 |
| **P4** | 消息/聊天系统 | 完整私信功能 |
| **P5** | 订单系统 | 完整交易流程 |
| **P6** | 我的发布/收藏/关于 | 辅助页面完善 |
| **P7** | 优化打磨（动画、空状态、错误处理） | 演示级品质 |

---

> **文档版本**：v1.0
> **创建日期**：2026-07-12
> **下一步**：用户评审确认后，产出详细实现计划 (`plan.md`)
